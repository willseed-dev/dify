import { ToolTypeEnum } from '../../workflow/block-selector/types';
type Props = {
    type?: ToolTypeEnum;
    isAgent?: boolean;
};
declare const Empty: ({ type, isAgent, }: Props) => any;
export default Empty;
