import type { ToolWithProvider } from '@/app/components/workflow/types';
type Props = {
    handleCreate: (provider: ToolWithProvider) => void;
};
declare const NewMCPCard: ({ handleCreate }: Props) => any;
export default NewMCPCard;
