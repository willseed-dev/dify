import type { ModelProvider } from '../declarations';
type CredentialPanelProps = {
    provider: ModelProvider;
};
declare const CredentialPanel: ({ provider, }: CredentialPanelProps) => any;
export default CredentialPanel;
