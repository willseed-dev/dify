import type { Option } from './types';
type ChunkStructureCardProps = {
    className?: string;
} & Option;
declare const ChunkStructureCard: ({ className, icon, title, description, effectColor, }: ChunkStructureCardProps) => any;
declare const _default: typeof ChunkStructureCard;
export default _default;
