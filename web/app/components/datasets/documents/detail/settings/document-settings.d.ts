type DocumentSettingsProps = {
    datasetId: string;
    documentId: string;
};
declare const DocumentSettings: ({ datasetId, documentId }: DocumentSettingsProps) => any;
export default DocumentSettings;
