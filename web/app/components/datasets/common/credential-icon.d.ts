import * as React from 'react';
type CredentialIconProps = {
    avatarUrl?: string;
    name: string;
    size?: number;
    className?: string;
};
export declare const CredentialIcon: React.FC<CredentialIconProps>;
export {};
