type NextStepButtonProps = {
    disabled: boolean;
    onClick: () => void;
};
/**
 * Reusable next step button component for dataset creation flow.
 */
declare function NextStepButton({ disabled, onClick }: NextStepButtonProps): any;
export default NextStepButton;
