type IProps = {
    show: boolean;
    onConfirm: () => void;
    onHide: () => void;
};
declare const StopEmbeddingModal: ({ show, onConfirm, onHide, }: IProps) => any;
export default StopEmbeddingModal;
