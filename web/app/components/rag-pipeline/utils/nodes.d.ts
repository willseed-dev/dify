import type { Viewport } from 'reactflow';
import type { Node } from '@/app/components/workflow/types';
export declare const processNodesWithoutDataSource: (nodes: Node[], viewport?: Viewport) => {
    nodes: Node[];
    viewport: any;
};
