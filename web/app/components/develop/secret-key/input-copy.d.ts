import * as React from 'react';
type IInputCopyProps = {
    value?: string;
    className?: string;
    children?: React.ReactNode;
};
declare const InputCopy: ({ value, className, children, }: IInputCopyProps) => any;
export default InputCopy;
