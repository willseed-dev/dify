import type { TimePickerProps } from '../types';
declare const TimePicker: ({ value, timezone, placeholder, onChange, onClear, renderTrigger, title, minuteFilter, popupClassName, notClearable, triggerFullWidth, showTimezone, placement, }: TimePickerProps) => any;
export default TimePicker;
