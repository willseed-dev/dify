import type { OnFeaturesChange } from '@/app/components/base/features/types';
type Props = {
    disabled: boolean;
    onChange?: OnFeaturesChange;
};
declare const FileUpload: ({ disabled, onChange, }: Props) => any;
export default FileUpload;
