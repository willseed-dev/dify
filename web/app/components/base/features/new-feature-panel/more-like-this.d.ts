import type { OnFeaturesChange } from '@/app/components/base/features/types';
type Props = {
    disabled?: boolean;
    onChange?: OnFeaturesChange;
};
declare const MoreLikeThis: ({ disabled, onChange, }: Props) => any;
export default MoreLikeThis;
