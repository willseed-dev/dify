import type { FileUpload } from '@/app/components/base/features/types';
type FileInputProps = {
    fileConfig: FileUpload;
};
declare const FileInput: ({ fileConfig, }: FileInputProps) => any;
export default FileInput;
