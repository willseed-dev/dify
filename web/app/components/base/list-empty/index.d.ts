import type { ReactNode } from 'react';
type ListEmptyProps = {
    title?: string;
    description?: ReactNode;
    icon?: ReactNode;
};
declare const ListEmpty: ({ title, description, icon, }: ListEmptyProps) => any;
export default ListEmpty;
