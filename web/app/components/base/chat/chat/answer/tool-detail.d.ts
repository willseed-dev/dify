import type { ToolInfoInThought } from '../type';
type ToolDetailProps = {
    payload: ToolInfoInThought;
};
declare const ToolDetail: ({ payload, }: ToolDetailProps) => any;
export default ToolDetail;
