type InputModeSelectProps = {
    value?: string;
    onChange: (value: string) => void;
};
declare const InputModeSelect: ({ value, onChange, }: InputModeSelectProps) => any;
export default InputModeSelect;
